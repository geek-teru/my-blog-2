---
/**
 * 準備中のページに置く箱。中身を書いたらこのコンポーネントごと消す。
 * ページ自体を用意しておかないと、メニューのリンクが 404 になってしまうため。
 */
---

<p class="wip">作成中です。</p>

<style>
	.wip {
		margin: 0;
		padding: 2.5rem 1.5rem;
		border: 1px dashed rgb(var(--gray-light));
		border-radius: var(--radius);
		background: var(--bg-subtle);
		color: rgb(var(--gray));
		text-align: center;
	}
</style>
